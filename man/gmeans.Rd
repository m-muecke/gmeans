% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmeans.R
\name{gmeans}
\alias{gmeans}
\title{G-means Clustering}
\usage{
gmeans(x, k_init = 1L, k_max = Inf, level = 0.05, ...)
}
\arguments{
\item{x}{numeric matrix of data, or an object that can be coerced to such a matrix
(such as a numeric vector or a data frame with all numeric columns).}

\item{k_init}{\code{integer(1)} initial amount of centers. Default is \code{1L}.}

\item{k_max}{\code{integer(1)} maximum amount of centers. Default is \code{Inf}.}

\item{level}{\code{numeric(1)} significance level for the Anderson-Darling test.
Default is \code{0.05}.}

\item{...}{additional arguments passed to \code{\link[stats:kmeans]{stats::kmeans()}}.}
}
\description{
Perform G-means clustering on a data matrix.
}
\examples{
x <- rbind(
  matrix(rnorm(100, sd = 0.3), ncol = 2),
  matrix(rnorm(100, mean = 1, sd = 0.3), ncol = 2)
)
colnames(x) <- c("x", "y")
cl <- gmeans(x)
}
\references{
Hamerly, Greg, Elkan, Charles (2003).
\dQuote{Learning the k in k-means.}
In Thrun S, Saul L, Schölkopf B (eds.), \emph{Advances in Neural Information Processing Systems}, volume 16.
\url{https://proceedings.neurips.cc/paper_files/paper/2003/file/234833147b97bb6aed53a8f4f1c7a7d8-Paper.pdf}.
}
