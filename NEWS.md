# gmeans (development version)

* The mlr3 integration vignette has been removed, since the G-means learner is now available directly in mlr3cluster as `lrn("clust.gmeans")`.
* `gmeans()` now errors when `k_init` exceeds `k_max`.

# gmeans 0.1.0

* Initial CRAN submission.
